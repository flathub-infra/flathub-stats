[tool.poetry]
name = "flathub-stats"
version = "0.1.0"
description = ""
authors = ["Bartłomiej Piotrowski <bpiotrowski@gnome.org>", "Kolja Lampe <razzeee@gmail.com"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
PyGObject = "^3.48.2"
argparse = "^1.4.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.1.1"
ruff = "^0.5.2"
faker = "^33.1.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.ruff]
lint.ignore = ["E501", "E402"]
lint.extend-select = ["I", "W", "UP", "TCH", "SIM"]
